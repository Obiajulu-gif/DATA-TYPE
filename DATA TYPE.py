number = input ("Type  a two digit Number\n")
# this section of code try to get the first and second subscript
first_digit = number[0]
second_digit = number[1]
# in this section we converted the string to integer
#and added the both of them which is the first and second input

print (int(first_digit) + int(second_digit))
